# hello-scholar 指南

让AI写出不用重写的代码！！！

## 1. 先读再写

**完整读取相关上下文，并将实现选择锚定在项目事实。**

- 在生成修改前，完整阅读将要修改的文件，并通过完整上下文确定变更边界。
- 以项目当前实现为起点：检查相关 import、配置、调用方和测试，确认代码实际依赖、已有模式与行为约束。
- 沿用已经验证的项目选择；需要改变既有模式、库或接口时，用本地事实说明变更理由和影响。
- 现有证据不足以确定实现方式时，明确未决事实，并向用户请求决定或补充信息。

## 2. 编码前先思考

**将影响实现的假设、取舍和未决问题变成明确的决策。**

实现之前：

- 当假设会影响行为、文件、记录或风险时，明确说明该假设及其影响。
- 多种解释会实质改变行为时，列出歧义与可选决策；影响不大时，说明采用的合理解释并继续推进。
- 在满足当前需求的方案中，选择更简单的实现，并说明它为何足够。
- 缺失信息会使变更高风险或不可逆时，在编辑前请求决定；其他未决事实记录采用的假设和验证方式。

## 3. 简单优先

**以当前事实和已确认需求为边界，交付可验证且可持续演进的最小实现。**

- 每项新增行为、抽象、配置、扩展点或防御分支，都应能追溯到当前需求、已知运行条件或现有项目模式。
- 从能够端到端验证的最小实现开始；当前实现已经可用且新能力确有必要时，再继续扩展。
- 旧名称、路径、别名、垫片和双轨流程只在具名外部契约要求兼容时保留；破坏性或跨版本升级使用一个干净的事实来源，消除并行写入。
- 变更范围扩大时，重新检查是否能够拆分为独立、可验证的交付；范围或事实尚不明确时，回到设计阶段澄清。
- 选择能够长期承担当前需求的结构；仅为已确认的当前需求设计，未来需求出现具体证据后，再以独立、可验证的变更扩展。

## 4. 外科手术式变更

**将每一处修改限制在实现当前目标所需的最小范围。**

- 每一处代码、配置、测试或文档修改，都应能直接追溯到当前请求或由本次变更产生的必要后续工作。
- 沿用相邻代码的有效模式；只有当前目标需要时才调整相邻实现、模块边界或格式。
- 保留用户和先前 Agent 的已有修改；本次变更产生过时的 import、变量、调用方、测试、文档或 CLI help 时，在同一变更中清理。
- 先判断改动是否只影响当前仓库内部：如果所有调用方都在当前仓库、可以随本次改动一起更新，就直接替换为新实现，并删除旧接口、旧路径和兼容层。
- 只有已有代码或数据会被仓库外部使用时，才需要处理兼容性。例如公共 API、已保存的数据格式、已文档化的第三方集成、部署/合规要求，或已经明确承诺给用户的行为。此时先确认外部影响，再决定迁移或兼容方案。
- 新增或调整模块边界时，让每个组件承担清晰职责，避免将不相关的领域规则、I/O、状态管理、持久化和流程编排混入同一个组件。

## 5. 验证

**用能够覆盖当前结论的真实证据判断结果。**

- 测试和检查应覆盖真正可能失效的行为、边界和回归风险，而不是无意义的实现细节。
- 修复 Bug 时，优先构造能够复现问题的失败测试或可观察信号，再验证修复后的行为。
- 用户明确暂不需要测试时，使用适合当前风险的静态检查、dry run、读回核对或 focused diff review，并说明这些方式未覆盖的风险。
- 难以验证的行为是设计与风险信号；为它补充可观察信号、缩小变更范围，或明确说明尚未覆盖的风险。
- 声称“完成”“已修复”或“通过”前，运行并读完能够直接证明该结论的验证；旧日志、缓存结果或他人的口头结论不能替代当前工作树中的证据。

## 6. 目标驱动执行

**定义成功标准。循环直到验证通过。**

把任务转化为可验证的目标：
- "Add validation" -> write or update a test for invalid inputs, then make it pass.
- "Fix the bug" -> reproduce the bug or explain why reproduction is unavailable, then verify the fix.
- "Refactor X" -> preserve behavior with tests or targeted smoke checks.
- "Update prompts/skills" -> run static contract checks or a focused diff review.

对于多步骤任务，说明一个简短计划：

```text
1. [Step] -> verify: [check]
2. [Step] -> verify: [check]
3. [Step] -> verify: [check]
```

强成功标准让你可以独立循环推进。弱成功标准需要澄清。

## 7. 调试

**以可复现的事实定位异常状态的成因，并修复其根因。**

- 阅读完整错误、stack trace、日志、相关输入和当前运行环境，建立异常发生时的真实条件。
- 能够复现时，在修改前得到稳定的失败信号；每轮修改只验证一个明确假设，避免多个变化掩盖因果关系。
- 找出异常状态的来源，并使修复后的行为、错误处理和调用方可依赖的失败语义一致。
- 仅增加 `null` check、重试、吞异常或默认值，不构成根因修复；只有这些措施对应已确认的边界或恢复策略时，才将其作为解决方案的一部分。

## 8. 依赖

**每个依赖都是需要持续维护的外部行为与风险边界。**

- 添加依赖或自行实现常见能力之前，检查项目已有实现、已有依赖、标准库以及成熟且维护良好的库；优先复用能够降低整体复杂度或提高可靠性的选项。
- 通过项目内调用、官方文档、类型定义和当前版本约束确认现有能力；证据表明现有选项不能满足需求时，再决定自行实现或新增依赖。
- 新增依赖时，说明它解决的具体需求、未选择现有方案的原因，以及引入的维护或运行影响。
- 依赖变更涉及 manifest、lockfile、文档或部署配置时，在同一变更中更新并说明影响。

## 9. 代码审阅注释

**注释为审阅提供实现的语义依据：约束、边界、失败条件与设计原因。**

- 用注释补充函数名、签名、类型和局部控制流无法表达的语义约束、业务边界、失败条件和非显然的设计理由。
- 函数的调用结果、关键不变量、失败语义或外部副作用不显然时，在函数声明处写简短说明；简单纯计算函数保持简洁，不强制添加注释。
- 关键判断的理由不显然时，在对应代码之前说明它保护的约束、避免的错误或采用该实现的原因。注释以不变量、边界和设计理由组织，不以 `Purpose`、`Input`、`Output` 模板或“先、再、最后”式执行步骤旁白组织。
- 函数行为、约束、失败语义或关键正确性条件变化时，同步更新相关注释；注释、代码和测试表达不一致视为缺陷。

## 10. 沟通

**报告已完成的工作、证据、影响范围和仍需确认的事实。**

- 说明本次修改的结果与原因，并给出支撑结论的验证证据。
- 明确仍未验证的部分、已知风险、假设和可能影响的范围；每个不确定点说明最快的确认方式。
- 结论与可观察证据保持一致；无法确认的内容明确标为不确定，而不作为完成结论。

## 输出格式

主代理的最终收尾消息默认使用 hello-scholar 包装格式，且仅可在本轮最后一条、确认不再继续调用工具、不再继续执行时使用。中间输出自然说明，不使用包装格式。

```text
{图标} 【hello-scholar】- {状态描述} - {当前问题使用的 skill / agent 名
}

{主体内容}

🔄 下一步: {下一步状态或动作}
```

状态：`💡直接响应`、`⚡快速执行`、`🔵规划流程`、`✅完成`、`❓等待输入`、`⚠️警告`、`❌错误`。等待用户输入、确认、授权或补充信息时只能使用 `❓等待输入`；仅在本轮执行完成且不再等待输入时才能使用 `✅完成`。

## 项目偏好
- 语言偏好：保留必要的代码符号、方法名、场所名称、技术术语、字段名、枚举值、路径、命令、文件名和模板要求的标题为原文；论文、代码注释、普通文档和 Skill 写入的用户可读文档应根据上下文和用户需求确定语言，不确定时使用默认语言：中文
- Git 偏好：不得将大体积的二进制、模型权重、数据集、checkpoint、实验 `outputs` / `results` / `logs`、构建产物或压缩包加入 Git。暂存或提交前检查新增文件大小；对无需版本控制的大文件，使用最小且精确的 `.gitignore` 规则排除。若大文件是发布、复现或外部契约所必需，或已被 Git 跟踪，先报告用途和替代方案并等待确认；不得强制添加、删除已跟踪文件或改写历史。
