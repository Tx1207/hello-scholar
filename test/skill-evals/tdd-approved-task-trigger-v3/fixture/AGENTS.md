# Config Upgrader Project Rules

- Read the current Spec, Plan, and Tasks before implementation.
- Execute only an approved/current Task that the user has separately authorized.
- Follow a Task's explicit `Process` field exactly.
- Keep the CLI dependency-free and preserve the valid version-2 output shape.
- Run focused tests and the complete `unittest` suite before updating Task completion.
