# Cache Strategy Service Rules

- Read current cache Specs and implementation before assigning design ownership.
- Candidate algorithms remain undecided until benchmark evidence and an explicit user decision exist.
- This documentation request must not change runtime code, package metadata, or tests.
- Use the absolute hello-scholar CLI for generated Indexes; never hand-edit their tables.
- Run `npm test` after documentation work.
