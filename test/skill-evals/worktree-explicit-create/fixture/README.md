# Cache Key Service

This package creates deterministic namespaced cache keys. A planned fix will make object-valued key parts independent of property insertion order, but this scenario asks only to prepare an isolated Worktree for that future implementation.
