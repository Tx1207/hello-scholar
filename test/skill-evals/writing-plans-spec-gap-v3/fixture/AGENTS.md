# Project Rules

- The accepted portions of `SPEC-026` define behavior; explicitly open decisions do not create defaults.
- Changes to public request headers, HTTP failure behavior, security policy, or legacy compatibility require an accepted Spec decision.
- Preserve constant-time HMAC comparison whenever verification behavior is changed.
- Run Node tests before reporting project changes.
