# Search Ranking Service Rules

- Read current Specs, ranking code, and tests before changing design records.
- `src/ranking.py` exposes the stable public ranking entry; do not modify code for a documentation-only request.
- Spec identity follows the problem and ownership boundary, not the date or proposed implementation technique.
- Generated Index files are written only by the absolute hello-scholar `docs sync` command.
- Run the full Python suite after documentation work to prove source bytes and behavior remain unchanged.
