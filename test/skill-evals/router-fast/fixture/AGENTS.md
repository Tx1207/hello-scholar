# Text Normalizer Project Rules

- Keep `normalize_text(value)` as the public entry point.
- Local implementation bugs may be fixed directly with a focused regression test.
- Do not create design documents for a local bug that does not change the interface.
- Do not start TDD unless the user or an approved task explicitly requests it.
- Run the complete standard-library test suite from the repository root.
