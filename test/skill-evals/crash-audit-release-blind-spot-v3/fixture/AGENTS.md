# Project Rules

- Run `node --test` before making claims about the release plan.
- Treat `README.md` as the public CLI contract and `fixtures/` as persisted user data.
- Do not edit files when the user requests a read-only audit.
- Cite concrete repository paths for compatibility or migration claims.
