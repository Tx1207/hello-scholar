# Project Rules

- Read `README.md`, both JSON Schemas, persisted fixtures, and both consumer examples before classifying compatibility.
- Run `node --test` before making claims about supported formats.
- Classify compatibility claims from repository evidence, including documentation, inputs, and observed consumers.
- A direction review is read-only and stops before migration design or implementation.
