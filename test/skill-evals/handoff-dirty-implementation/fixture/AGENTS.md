# 搜索规范化项目规则

- 仓库面向用户的文档默认使用中文，代码标识符、路径和命令保持原文。
- 实现必须服从当前 Accepted Spec、Approved Plan 和已批准的 Current Tasks。
- 不得把 staged、modified 和 untracked 混成一句“有未提交改动”；必须分别核对。
- 用户只要求 Handoff 时，不继续实现、不改 Tasks、不改变 Git index、不提交。
- Handoff 应引用事实源并脱敏，不能复制整份 Spec、Plan、Tasks、diff 或 session note。
