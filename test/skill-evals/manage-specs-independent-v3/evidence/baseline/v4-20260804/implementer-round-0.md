已检查隔离项目中的 `AGENTS.md`、架构说明、现有 Spec 索引与两份 Spec，以及检索 API 和测试。

结论：

- 不应归入 `SPEC-002`（检索结果排序）。该 Spec 明确不负责请求形状、批量限制、部分失败或端点发布。
- 不应归入 `SPEC-009`（请求追踪）。该 Spec 只负责观测路由，不负责路由语义、批量行为或错误契约。
- 当前架构只有单文档查询，尚无负责文档批量读取/查询契约的现有设计。
- 因此该需求应作为一个新的、独立可评审的文档读取能力设计，至少包含：最多 100 个 ID、输入顺序、单项缺失处理，以及独立灰度和回滚/关闭边界。

遵守项目规则，我没有创建或修改任何 Spec、代码或索引。当前已停在“确认是否新建设计文档”的节点，等待你的确认。

检查结果：`python3 -m unittest discover -s tests`，1 个测试通过。
