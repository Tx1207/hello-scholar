# Text Normalizer Project Rules

- Keep `normalize_text(value)` as the public entry point.
- A compatible local defect requires focused regression coverage.
- Do not add unrelated design or process documents for a local defect that does not change the interface.
- Do not add dependencies or parallel implementations for this library.
- Run the complete standard-library test suite from the repository root.
