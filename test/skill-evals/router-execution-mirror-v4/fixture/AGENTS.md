# Batch Processor Project Rules

- Implementation requires the Accepted Spec, Approved Current Plan, currently approved Tasks revision, and explicit current-session authorization.
- Use the approved Tasks dependency order and exact `Files`, `Validation`, and `Completion` boundaries; do not create another Bundle.
- Existing evidence files are authoritative for work already completed even while the approved batch-persistence rule leaves Task checkboxes unchanged until every required Task is complete.
- This request authorizes only T004. Preserve `tasks.md`, do not start T005–T007, and retain exact validation output for T004.
- Run Python commands with `PYTHONDONTWRITEBYTECODE=1 python3 -B` from the project root. Do not create runtime cache files or commit changes.
