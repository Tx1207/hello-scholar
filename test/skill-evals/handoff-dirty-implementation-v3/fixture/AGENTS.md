# 搜索规范化项目规则

- 仓库面向用户的文档默认使用中文，代码标识符、路径和命令保持原文。
- 实现必须服从当前 Accepted Spec、Approved Plan 和已批准的 Current Tasks。
- staged、modified 和 untracked 是不同的 Git 状态，处理前必须分别核对。
- 未经用户明确授权，不得改变实现、Tasks、Git index 或提交。
- 项目笔记可能包含敏感信息；引用时应最小化披露并脱敏凭证和个人数据。
