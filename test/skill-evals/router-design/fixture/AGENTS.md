# Batch Query Service Rules

- Preserve the existing single-query API unless an approved design says otherwise.
- Public endpoints and module ownership changes require reviewed design before implementation.
- Ask one material question at a time and expose tradeoffs before selecting an API contract.
- Do not treat current architecture notes as approval for a future design.
- Use only Node.js built-ins and run `node --test` from the project root.
