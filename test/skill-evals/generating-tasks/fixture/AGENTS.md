# Feature Policy Engine Rules

- Treat the Accepted Spec as behavior scope and the Approved Plan as the implementation strategy.
- This request produces only `tasks.md`; do not implement, approve Tasks, or change upstream documents.
- A Task must be independently readable with exact files, commands, and observable completion.
- TDD is used only where the Approved Plan explicitly selects it.
- Generated Indexes are written only by the absolute hello-scholar CLI. Run all Python tests after document work.
