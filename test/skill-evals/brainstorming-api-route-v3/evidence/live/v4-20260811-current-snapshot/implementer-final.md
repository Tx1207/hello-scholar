已按审核通过的精确内容写入：

```text
hello-scholar/specs/batch-retrieval/SPEC-014-public-batch-retrieval-api/spec.md
```

结果：

- `SPEC-014` 保持 `status: draft`、`revision: 1`；
- 包含七个核心章节、候选方案与权衡、材料性风险与控制；
- 已运行 `hello-scholar docs sync`；
- 已生成并验证全局及 `batch-retrieval` Topic Index；
- `docs check`：3 个 Specs、0 个错误；
- 未创建 `plan.md`、`tasks.md` 或源码。

下一步 owner 是 `$writing-plans`。其输入是当前已写入但仍为 draft 的 `SPEC-014`；由于 Spec 尚未 accepted，规划 owner 应先报告状态并停止，不应创建 `plan.md`。
