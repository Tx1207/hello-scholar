当前事实：

- `src/retrieval-service.js` 只有单文档 `getDocument(id)`。
- `clients/export-client.js` 目前按输入顺序逐个调用，并为每个输入 ID 返回一个结果；缺失文档表示为 `document: null`。
- 客户端当前拥有按 ID 重试逻辑。
- `SPEC-013` 只规定请求预算，不规定单次请求与批量请求的成本或响应契约。
- `SPEC-007` 只规定副本读一致性，不规定批量响应形状或部分失败。
- 当前没有批量接口或异步作业契约。

请确认一个兼容性边界：**无论最终选择同步批量入口、异步作业，还是继续由客户端聚合，是否都必须保持“每个输入 ID 恰好一个结果、结果顺序与输入一致、缺失项显式返回”的现有语义（包括重复 ID）？**
