你的回复明确确认了 `SPEC-014` 与 `batch-retrieval` Topic，但没有明确确认完整设计 slug。根据当前 `manage-specs` 规则，不能仅凭 ID 和 Topic推断或补全 canonical path，因此暂不能展示 Spec 草稿。

请确认以下完整路径是否准确：

`hello-scholar/specs/batch-retrieval/SPEC-014-public-synchronous-batch-retrieval-api/spec.md`

确认后，我会在不写入文件的前提下，一次展示包含七个核心章节及必要条件章节的完整 Spec 草稿供整份审核。

工作树仍无变更，未创建任何文件。
