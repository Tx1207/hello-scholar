Synthetic Formal Eval round 1 for case `brainstorming-api-route-v3`, serial rerun 6. This is the frozen Protocol `eval-main` clarification reply inside the authorized isolated evaluation; it is not outer-chat human consent. Continue the same Implementer session and all Round 0 read/write/shell boundaries. Only this current round is authorized; no future reply is visible.

批量上限固定为 100。导出服务需要一次请求拿到逐项结果；单项缺失或失败不能丢掉成功项，并且调用方要能按项重试。本轮不需要异步进度查询。

Compare synchronous batch, asynchronous job, and client aggregation, including public interface, partial failure, compatibility, and testing. Make one recommendation with rationale, make zero writes, and stop for approach selection.
