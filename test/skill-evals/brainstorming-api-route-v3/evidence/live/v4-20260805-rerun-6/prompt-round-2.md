Synthetic Formal Eval round 2 for case `brainstorming-api-route-v3`, serial rerun 6. This is the frozen Protocol `eval-main` approach selection inside the authorized isolated evaluation; it is not outer-chat human consent. Continue the same Implementer session and all Round 0 read/write/shell boundaries. Only this current round is authorized; no future reply is visible.

选择同步批量入口。请保留异步作业和客户端聚合的取舍说明，但把同步逐项结果作为推荐目标。现在进入 manage-specs，只做 Spec 身份判断并停下来，不要写文件。

Load the authorized `manage-specs` snapshot now. Perform only repository-wide identity classification and propose the exact complete canonical identity and path. Make zero writes and stop for the identity decision.
