# Invalid Evaluator Orchestration

This concurrent attempt is diagnostic only and is not a Formal Eval pass or fail.

The harness completion notifications associated case descriptions and Agent IDs inconsistently. Eval main consequently delivered the two later Protocol messages to the opposite case sessions. Both Agents correctly rejected the unrelated message. No project bytes changed, no Reviewer was launched, and no Scorecard was created.

The replacement run is strictly serial: one case completes its full Implementer→Reviewer chain before the second case starts.
