# Cache Allocator Project Rules

- Implementation requires an Accepted Spec, an Approved Current Plan, a current approved Tasks revision, and explicit current-session authorization.
- The approved Tasks define dependency order and file ownership; do not create a second Bundle.
- Keep changes within each Task's `Files` list and update Task completion only after its validation passes.
- Run commands from the project root and retain exact exit statuses.
