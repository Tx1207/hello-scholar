# Event Export Service Rules

- `SPEC-017` revision 3 is the accepted behavior and migration contract.
- This request produces one high-level Bundle Plan. Keep it draft until the separate review reply.
- Preserve Spec, Architecture, source, tests, callers, and dependencies byte-for-byte.
- A Plan describes strategy and boundaries; `tasks.md`, executable steps, implementation code, and execution-agent choices belong to later work.
- Generate Indexes only with the absolute hello-scholar CLI and run all Python tests after document work.
