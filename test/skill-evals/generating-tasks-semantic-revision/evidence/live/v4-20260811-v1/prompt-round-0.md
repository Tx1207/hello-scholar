You are the Implementer for isolated Formal Eval case `generating-tasks-semantic-revision`, Live round 0. Work only in `/tmp/hello-scholar-eval-generating-tasks-semantic-revision-live-v1-20260811a`.

Every shell command must explicitly use that working directory (`git -C ...` or `env -C ...`); shell state does not persist.

Current user message (verbatim):
请根据当前 Accepted Spec 和 Approved Plan 修订现有 `tasks.md`：保留已完成且仍有效的 `T001`、checkbox 和证据；删除未完成且已废弃的 `T002`；`T003` 目标不变，只更新 Work、Validation 和 Completion；为新增 audit integration 使用新的更大 Task ID。修复依赖图并把整份 Tasks 重置为 pending-review，等我审核。不要改写过去执行事实，不要重排或复用 ID，也不要开始实施。

Project rules are in `/tmp/hello-scholar-eval-generating-tasks-semantic-revision-live-v1-20260811a/AGENTS.md`.

The only external Skill you may read is the current explicit-file snapshot:
- `/tmp/hello-scholar-eval-snapshots/generating-tasks-semantic-revision-live-v1-20260811a/generating-tasks` with tree SHA-256 `47d01e11940af612cbca1ca2117b159433aafa88a81ea1f041cfb4fcaad56b06`.
Read and follow it.

Reading boundary: do not read `/xsb/hello-scholar/.claude/worktrees/document-revision-contract-v2` or any other repository/Eval directory. You may execute the absolute hello-scholar CLI but must not inspect its source. Do not look for raw Scenario, raw Protocol, rubrics, hard rejects, expected answer, baseline evidence, surrounding conversation, or other Eval artifacts.

Read project rules, Architecture, the complete Accepted Spec revision 3, Approved Plan revision 2, current Tasks, current indexes, source, tests, saved execution evidence, and confirmable Git history before editing. Treat current Tasks as Baseline and upstream documents plus this explicit request as Authority/Delta. Apply the requested field boundary literally: for surviving T003, keep its ID, goal, Spec Coverage, Files, and all other nondependency fields byte-for-byte except `Work`, `Validation`, and `Completion`; change `Depends On`/`Parallel` only as required to repair the DAG after T002 removal and new integration work. Preserve T001 ID, checked state, and execution evidence exactly. Remove T002 without reusing its ID. Allocate the new obligation an unused ID greater than all Baseline and confirmable historical IDs. Rebuild coverage and the DAG without changing unauthorized surviving-task fields. Increment Tasks revision once, bind current upstream revisions, and reset exactly to `approval: pending-review`, `approved_revision: null`, `status: pending`. Write only `tasks.md` plus Indexes generated by the approved absolute CLI. Run the Protocol-approved commands, verify upstream and implementation bytes remain unchanged, prove no runtime artifacts, present the complete Tasks revision, and stop for review without approval or implementation.
