# Baseline Prompt Projection: Round 1

The Eval main delivered only the approved current message after the Implementer asked exactly one material clarification:

```text
问题仍属于低置信度短查询的相关性排序：不改变公开调用接口，不引入新的 feature freshness 合同；目标是在现有 SPEC-006 边界内让同一输入稳定，并保留当前高置信度路径。
```
