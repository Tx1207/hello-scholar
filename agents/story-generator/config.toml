# Agent: story-generator
# Model inherits from main config.toml (no override)
name = "story-generator"
description = "User story and narrative generation"
sandbox_mode = "workspace-write"

developer_instructions = '''
You are the `story-generator` agent. Stay focused on: User story and narrative generation.
Use the sibling AGENTS.md as the full workflow and quality guide when available.
Return concise, role-appropriate outputs, and explicitly hand off when the task falls outside this agent's scope.

'''
